# 🔧 Решение проблемы с Telegram ботом (409 Conflict)

## ❌ Проблема

Ошибка: `409 Conflict: terminated by other getUpdates request; make sure that only one bot instance is running`

Это означает, что **несколько экземпляров бота** пытаются одновременно получать обновления от Telegram API.

---

## ✅ Решение

### Шаг 1: Остановите локальные экземпляры

На вашем **локальном компьютере** (не на сервере!):

```bash
# Остановите Docker контейнеры
docker compose down

# Проверьте, что нет запущенных контейнеров
docker ps

# Если есть процессы с node или ts-node
pkill -f "ts-node"
pkill -f "node.*server"
```

### Шаг 2: Проверьте другие серверы

Если бот был запущен на других серверах или в других местах - остановите их тоже.

### Шаг 3: Используйте Webhook вместо Polling (рекомендуется для продакшена)

Проблема возникает при использовании **polling** режима (постоянные запросы к Telegram API).
Для продакшена лучше использовать **webhook** режим.

#### Как переключиться на webhook:

1. Откройте `server/telegram.ts` или где настроен бот
2. Вместо `bot.startPolling()` используйте webhook
3. Настройте Nginx для проксирования webhook запросов

### Шаг 4: Сброс webhook (временное решение)

Если нужно быстро исправить, сбросьте webhook:

```bash
# Используйте curl для сброса
curl -X POST "https://api.telegram.org/bot7553529520:AAEa-KzgBZdjhxDsLpMI9JZOA27NP2c3pz0/deleteWebhook?drop_pending_updates=true"
```

### Шаг 5: Перезапустите бот на сервере

```bash
# На сервере
cd ~/autoproject
docker compose restart api

# Проверьте логи
docker compose logs -f api
```

---

## 🔒 Безопасность: Скройте токен!

⚠️ **ВАЖНО**: Токен бота **не должен быть в открытом доступе**!

Сейчас токен виден в:
- Логах
- Истории команд
- Этом файле

### Что нужно сделать:

1. **Создайте новый токен бота** через @BotFather в Telegram
2. Обновите `.env` файл на сервере с новым токеном
3. **НЕ коммитьте** токены в Git
4. Используйте только переменные окружения

---

## 📊 Проверка

После исправления проверьте:

```bash
# На сервере
docker compose logs -f api
```

Вы должны увидеть:
```
✅ Telegram bot initialized
✅ Server listening on port 5000
```

Без ошибок `409 Conflict`.

---

## 🛠️ Альтернатива: Один бот на несколько инстансов

Если нужно запускать бот в нескольких местах, используйте:
- Разные токены для разных окружений (dev, staging, prod)
- Webhook режим для продакшена
- Polling только для локальной разработки

