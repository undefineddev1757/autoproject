# 🚀 Руководство по деплою на Ubuntu 22.04

Полная инструкция по развертыванию приложения autoshopone на сервере Ubuntu 22.04.

## 📋 Предварительные требования

- Сервер с Ubuntu 22.04
- Доступ по SSH
- Доменное имя (опционально, но рекомендуется для SSL)
- Root или sudo доступ

---

## 🔧 Шаг 1: Подготовка сервера

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите необходимые пакеты
sudo apt install -y git curl wget nano ufw

# Настройте firewall
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw --force enable
```

---

## 🐳 Шаг 2: Установка Docker

```bash
# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
rm get-docker.sh

# Добавьте текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Примените изменения (или перелогиньтесь)
newgrp docker

# Проверьте установку
docker --version
docker compose version
```

---

## 📦 Шаг 3: Клонирование и настройка проекта

```bash
# Перейдите в домашнюю директорию
cd ~

# Клонируйте репозиторий (замените URL на ваш)
git clone <URL_вашего_репозитория> autoshopone
cd autoshopone

# Создайте .env файл
cp .env.example .env
nano .env
```

Заполните `.env` файл:

```env
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

---

## 🚀 Шаг 4: Первый запуск

```bash
# Сделайте скрипт деплоя исполняемым
chmod +x deploy.sh

# Запустите приложение
docker compose up -d --build

# Проверьте статус
docker compose ps

# Просмотр логов
docker compose logs -f
```

Приложение будет доступно:
- **Web (Next.js)**: http://your-server-ip:3000
- **API**: http://your-server-ip:5000

---

## 🌐 Шаг 5: Настройка Nginx (опционально, но рекомендуется)

### Установка Nginx

```bash
sudo apt install -y nginx
```

### Настройка конфигурации

```bash
# Создайте конфигурационный файл
sudo nano /etc/nginx/sites-available/autoshopone
```

Скопируйте содержимое из файла `nginx.conf` в этот файл, заменив `ваш_домен.com` на ваш реальный домен.

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/autoshopone /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

---

## 🔒 Шаг 6: Настройка SSL с Let's Encrypt (для продакшена)

```bash
# Установите Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получите SSL сертификат (замените на ваш домен)
sudo certbot --nginx -d ваш_домен.com -d www.ваш_домен.com

# Проверьте автообновление сертификата
sudo certbot renew --dry-run
```

Certbot автоматически обновит вашу конфигурацию Nginx для работы с HTTPS.

---

## 🔄 Автоматическое обновление

### Настройка автоматического перезапуска Docker

```bash
# Отредактируйте docker-compose.yml, добавив restart: always
docker compose down
nano docker-compose.yml
```

Добавьте `restart: always` к каждому сервису:

```yaml
services:
  web:
    restart: always
    # ... остальная конфигурация

  api:
    restart: always
    # ... остальная конфигурация
```

```bash
# Перезапустите контейнеры
docker compose up -d
```

---

## 📊 Полезные команды

### Управление приложением

```bash
# Просмотр логов
docker compose logs -f

# Просмотр логов конкретного сервиса
docker compose logs -f web
docker compose logs -f api

# Остановка
docker compose down

# Запуск
docker compose up -d

# Перезапуск
docker compose restart

# Обновление приложения
./deploy.sh
```

### Мониторинг

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Проверка работы Nginx
sudo systemctl status nginx

# Логи Nginx
sudo tail -f /var/log/nginx/autoshopone_access.log
sudo tail -f /var/log/nginx/autoshopone_error.log
```

### Очистка

```bash
# Очистка неиспользуемых ресурсов Docker
docker system prune -a

# Удаление старых образов
docker image prune -a
```

---

## 🐛 Решение проблем

### Порты заняты

Если порты 3000 или 5000 заняты:

```bash
# Проверьте, что использует порт
sudo lsof -i :3000
sudo lsof -i :5000

# Убейте процесс
sudo kill -9 <PID>
```

### Проблемы с правами доступа

```bash
# Дайте права на директорию data
sudo chown -R $USER:$USER ~/autoshopone/data
chmod -R 755 ~/autoshopone/data
```

### Контейнеры не запускаются

```bash
# Полная пересборка
docker compose down -v
docker system prune -a -f
docker compose up -d --build
```

### Проблемы с Telegram Bot

Проверьте:
1. Корректность токена бота в `.env`
2. Правильность chat_id
3. Логи контейнера: `docker compose logs -f api`

---

## 🔐 Безопасность

### Базовая защита

```bash
# Установите fail2ban для защиты от брутфорса SSH
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban

# Отключите root-логин по SSH
sudo nano /etc/ssh/sshd_config
# Установите: PermitRootLogin no
sudo systemctl restart sshd
```

### Регулярные обновления

```bash
# Создайте cron job для автоматических обновлений
sudo crontab -e
```

Добавьте строку для еженедельного обновления (каждое воскресенье в 3 AM):

```cron
0 3 * * 0 cd /home/yourusername/autoshopone && git pull && docker compose up -d --build
```

---

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [Nginx Documentation](https://nginx.org/ru/docs/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)
- [Ubuntu Server Guide](https://ubuntu.com/server/docs)

---

## 💡 Советы

1. **Регулярно делайте бэкапы** директории `data/` - там хранятся данные LowDB
2. **Мониторьте логи** с помощью `docker compose logs -f`
3. **Настройте мониторинг** (например, Uptime Kuma или StatusCake)
4. **Используйте CI/CD** для автоматического деплоя (GitHub Actions, GitLab CI)
5. **Регулярно обновляйте** зависимости и Docker образы

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker compose logs -f`
2. Проверьте статус: `docker compose ps`
3. Проверьте Nginx: `sudo nginx -t`
4. Проверьте firewall: `sudo ufw status`

---

**Успешного деплоя! 🎉**

